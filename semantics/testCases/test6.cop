Given an parabola y=x^2+2 and a line x+y=5. Let (1,3) and (-1,3) be two points on the parabola. Find the distance between the points where the tangents at points meets the given line.


line_int : line l1 , line l2 -> point 
<<
    point temp
    if(slope(l1) == slope(l2))
    <<
        output : "Given line is parallel to the tanget so no intersection exits "
    >>
    else 
    <<
        temp : (f(l1)*c(l2)-f(l2)*c(l1))/(g(l1)*f(l2)-g(l2)*f(l1)) ,  (g(l2)*c(l1)-g(l1)*c(l2))/(g(l1)*f(l2)-g(l2)*f(l1))
    >>
    exit : temp
>>   


main -> int 
<<
    parabola eqn : 1, 0, 0, 0, -½, 2
    line l : 0, 0, 0, 1, 1, -10
    point p1 : (1,3)
    point p2 : (-1,3) 
    line l1 , l2 
    l1 = tangent ( eqn , p1 )
    l2 = tangent ( eqn , p2 )
    point p1 = line_int ( l1 , l)
    point p2 = line_int ( l2 , l)
    float dis = ((q1.x - q2.x)^2 + (q1.y - q2.y)^2)^(1/2)
    output: “The distance between the points where the tangents at 
    points meets the given line is:”, dis
    exit: 0
>>
